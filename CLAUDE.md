# 이 리포를 받은 AI 에이전트에게 (Claude Code · Codex · Antigravity 공통)

사용자가 이 리포 주소를 주면서 **"세팅해줘" / "설치해줘"** 라고 하면 아래 순서대로 한다.
사용자는 코딩을 모르는 영업사원이라고 가정한다. 한국어로, 기술 용어 없이, 한 번에 한 가지씩 안내한다.

## 하는 일
1. 리포를 내려받는다(이미 폴더 안이면 생략).
   `git clone https://github.com/linchpinisland/sales-crm.git && cd sales-crm`
   git이 없으면 GitHub의 "Download ZIP"을 받아 풀게 한다.
2. `node setup.mjs` 를 실행한다. 이 스크립트가 구글 시트 생성 → 코드 넣기 → 웹앱 배포 → 바탕화면에 `고객관리-클로드폴더` 만들기까지 한다.
3. 종료코드 **2** 로 멈추면 화면의 "사람이 할 일"을 사용자에게 그대로, 쉬운 말로 전달하고 끝났다는 답을 기다린 뒤 **같은 명령을 다시 실행**한다. 멈추는 경우는 세 가지뿐이다.
   - clasp 없음 → `npm install -g @google/clasp` (Node가 없으면 https://nodejs.org 의 LTS 설치부터)
   - 구글 로그인 안 됨 → `clasp login` (브라우저에서 계정 선택 → 허용)
   - Apps Script API 꺼짐 → https://script.google.com/home/usersettings 에서 "사용". **clasp login 한 계정과 같은 계정인지** 꼭 확인시킨다(계정이 여러 개면 여기서 자주 틀린다).
4. 종료코드 **0** 이면 출력된 **구글 시트 주소**를 열어 주고(`open` / `start`), 마지막 사람 몫 네 줄을 안내한다.
   - 시트 메뉴 [고객관리] → [① 초기 설정 실행] → 승인(고급 → 이동 → 허용) → 한 번 더 실행.
   - "확인되지 않은 앱" 경고는 본인이 만든 스크립트를 본인이 쓰는 것이라 뜨는 표준 안내라고 설명한다.
5. 끝나면 세 가지를 알려준다: 구글 시트 주소, 휴대폰용 웹앱 주소(홈 화면에 추가 권장), 바탕화면 `고객관리-클로드폴더` 사용법("CSV 세 개 넣고 → 이번 주 챙길 고객 뽑아줘").

## 하지 말 것
- 사용자의 구글 비밀번호·인증 코드를 묻거나 대신 입력하지 않는다. 로그인과 "허용"은 사용자가 브라우저에서 직접 한다.
- `src/appsscript.json` 의 `webapp.access` 를 `MYSELF` 외의 값으로 바꾸지 않는다(본인만 접근).
- 실제 고객 데이터를 리포 폴더에 넣거나 커밋하지 않는다.
- 카카오톡 연동은 선택 사항이다. 사용자가 먼저 원할 때만 `docs/카카오연동.md` 로 안내한다. 기본 알림은 이메일이다.

## 터미널이 부담스럽다는 사용자에게 (대안, 설치 프로그램 없음)
`docs/설치가이드.md` 1단계의 **템플릿 사본 만들기** 링크를 열어 주고 가이드 1~3단계를 같이 진행한다.

## 다시 실행 · 문제 해결
- `node setup.mjs` 는 여러 번 실행해도 시트를 새로 만들지 않는다(`.clasp.json` 이 있으면 이어서 진행). 새로 만들려면 `.clasp.json` 과 `.setup-state.json` 을 지운다.
- 진행 상태 확인: `node setup.mjs --status`
- 코드를 고친 뒤 반영: `node setup.mjs` (같은 웹앱 주소로 새 버전 배포)
